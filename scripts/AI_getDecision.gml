// AI_getDecision(contestant, spell.attack)
var contestant = argument0;
var attack = argument1;
var epsilon = 0.0001;

if (attack > 0) {
    value = ( attack / (opponent.hp + epsilon) )
    if (value > damageSpellThreshold)
      then return true;
}


